#!/usr/bin/env ruby
$:.unshift File.expand_path('../../lib', __FILE__)
require "was_tracer"

output_file = ARGV[0]
trace_file = ARGV[1] || 'trace.log'
parse_percent = (ARGV[2] || 100).to_i

t = WasTracer::Trace.new(trace_file, parse_percent)
t.render_html_frames(output_file)